"""
설명 : for 문을 사용해봤습니다.
code : for, sum, range
"""


def solution(a, b):
    answer = sum(range(a,b + 1))
    if a > b:
        for i in range(b, a+1):
            answer += i
        return answer
    return answer
